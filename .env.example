# Optional: enable Gemini-generated plan language and candidate ranking.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash

# Optional: enrich place previews with live provider data.
GOOGLE_PLACES_API_KEY=
YELP_API_KEY=

# Optional: force the resilient curated POI dataset (also used by E2E tests).
# LAYOVERPLUS_POI_MODE=curated
